@{
    Layout = "_Layout_b5";
    // 判斷是否是 iframe 請求（用 QueryString 或 Header）
    var isIframe = Context.Request.Query["isIframe"].ToString().ToLower() == "true";
    if (isIframe)
    {
        Layout = "_Layout3";
    }
}
